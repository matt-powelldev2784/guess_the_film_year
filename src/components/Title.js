import styled from 'styled-components';

const Title = () => {
  return <PageTitle>Guess the film Year!?</PageTitle>;
};

export default Title;

const PageTitle = styled.h1`
  font-size: 3.5rem;
  color: white;
  background-color: #008cba;
`;
